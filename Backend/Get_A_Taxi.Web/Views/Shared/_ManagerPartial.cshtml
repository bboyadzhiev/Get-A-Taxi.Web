@using Get_A_Taxi.Web.Infrastructure.LocalResource
@using Microsoft.AspNet.Identity
@using Get_A_Taxi.Models
@if (Request.IsAuthenticated && User.IsInRole(UserRoles.Manager.ToString()))
{
    <ul class="nav navbar-nav">
        <li>@Html.ActionLink(Resource.Manager, "Index", "Main", new { area = "Management" }, htmlAttributes: new { id = "managerLink" })</li>
    </ul>
}